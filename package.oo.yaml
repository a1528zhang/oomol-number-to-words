version: 0.1.1
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    npm install
icon: ./icon.png
name: number-to-words
description: translate number to words.
displayName: Number To Words
releaseNotes: "fix: upload icon"
repository: https://github.com/a1528zhang/oomol-number-to-words
